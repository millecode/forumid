{% extends 'admin/base_admin.html.twig' %}

{% block admin_content %}
	<div class="card-soft p-4">
		<div class="fw-extrabold text-slate-900 mb-2">{{ page_title }}</div>
		<p class="text-slate-600 mb-0" style="line-height:1.8;">
			Page en construction — la structure est prête.
		</p>
	</div>
{% endblock %}
